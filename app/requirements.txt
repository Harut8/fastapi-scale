# FastAPI Scaling Demo - Python Dependencies
#
# Install:
#   pip install -r requirements.txt
#
# Install with development tools:
#   pip install -r requirements.txt -r requirements-dev.txt

# Core framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# ASGI server optimizations
uvloop==0.19.0              # 2-4x faster event loop
httptools==0.6.1            # Fast HTTP parsing

# Pydantic for data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Metrics and monitoring
prometheus-client==0.19.0

# HTTP client (for health checks, external calls)
httpx==0.25.2

# Optional: Database drivers (uncomment as needed)
# asyncpg==0.29.0           # PostgreSQL async driver
# psycopg2-binary==2.9.9    # PostgreSQL sync driver
# redis==5.0.1              # Redis client
# aio-pika==9.3.1           # RabbitMQ async client

# Production server
# Already included: uvicorn, gunicorn
